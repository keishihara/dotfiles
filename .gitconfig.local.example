# ~/.gitconfig.local -- Machine-specific git overrides (not tracked by git)
# Copy this file to ~/.gitconfig.local and edit as needed.
# Settings here override the main .gitconfig (loaded via [include]).

# [user]
#     name = Your Name
#     email = you@company.com

# [credential "https://github.com"]
#     helper =
#     helper = !/usr/bin/gh auth git-credential
# [credential "https://gist.github.com"]
#     helper =
#     helper = !/usr/bin/gh auth git-credential
